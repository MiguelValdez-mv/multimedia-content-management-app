export const CONTENT_TYPES = {
  TEXT: "TEXT",
  IMAGE: "IMAGE",
  VIDEO_URL: "VIDEO_URL",
};

export const CONTENT_TYPES_ENUM = Object.values(CONTENT_TYPES);

export const USER_ROLES = {
  ADMIN: "ADMIN",
  CREATOR: "CREATOR",
  READER: "READER",
};

export const USER_ROLES_ENUM = Object.values(USER_ROLES);
